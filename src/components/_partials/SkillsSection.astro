---
import { Image } from "astro:assets";
import Subtitle from "../Subtitle.astro";
import hand from "../../assets/illustrations/hand.svg";

const skillsItems = [
  {
    icon: hand,
    title: "Full-Stack Product Builder",
    description:
      "I have worn every R&D hat though my career. Product Manager, Designer, and now curious front-end developer.",
  },
  {
    icon: hand,
    title: "Strategic Business Partner",
    description:
      "I once was part of a team of 2 int a startup where I learned the meaning of truly acting like an owner. I know the pains of low MRRs and high churn.",
  },
  {
    icon: hand,
    title: "Systems Thinker",
    description:
      "I’ve worked for startups of all sizes across many categories. Specializing in technical and complex products.",
  },
  {
    icon: hand,
    title: "Designer Super IC and Lead",
    description:
      "I will never not want to touch pixels. Design is my new art tool, but I have often been placed in positions of leadership to upskill teams.",
  },
];
---

<div class="section-container">
  <Subtitle title="Some of My Skills" />
  <div class="split-container">
    {
      skillsItems.map(({ icon, title, description }) => (
        <div class="column">
          <div class="art-content">
            <Image src={icon} alt={title} />
          </div>
          <div class="text-content">
            <h4>{title}</h4>
            <p>{description}</p>
          </div>
        </div>
      ))
    }
  </div>
</div>

<style lang="scss">
  .split-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-base);
    padding-top: var(--spacing-base);

    @include tablet {
      grid-template-columns: 1fr;
    }

    .column {
      display: flex;
      flex-direction: row;
      gap: var(--spacing-small);
      padding: var(--spacing-small);

      border-bottom: 1px solid var(--color-border);

      .art-content {
        display: flex;
        justify-content: center;
        align-items: center;

        width: 40px;
        min-width: 40px;
        max-width: 40px;
        min-height: 40px;
        max-height: 40px;
        height: 40px;
      }

      .text-content {
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: var(--spacing-small);

        padding-top: var(--spacing-small);

        h4 {
          margin-bottom: var(--spacing-xxs);
        }
      }
    }
  }
</style>
